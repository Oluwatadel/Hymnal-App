<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Hymn_Book.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Hymn_Book"
    Shell.FlyoutBehavior="Flyout"
    Title="Hymn_Book">

    <FlyoutItem Title="Home" Icon="Home.png">
        <ShellContent ContentTemplate="{DataTemplate views:MainPage}" />
    </FlyoutItem>
        

    <FlyoutItem Title="Favoruite" Icon="Favourite.png">
        <ShellContent ContentTemplate="{DataTemplate views:HymnSearchPage}" />
    </FlyoutItem>

    <!-- 👉 Add any other pages you want listed in the menu... -->
    <FlyoutItem Title="About" Icon="contact.png">
        <ShellContent ContentTemplate="{DataTemplate views:AboutPage}" />
    </FlyoutItem>
</Shell>